import{H as e,gt as t,nt as n,tt as r}from"./chunks/framework.sms2Rnvf.js";var i=JSON.parse(`{"title":"Searching encrypted project data","description":"","frontmatter":{},"headers":[],"relativePath":"insights/project-search.md","filePath":"insights/project-search.md","lastUpdated":1781102819000}`),a={name:`insights/project-search.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<div style="display:none;" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /insights/project-search.md for this page in Markdown format</div><h1 id="searching-encrypted-project-data" tabindex="-1">Searching encrypted project data <a class="header-anchor" href="#searching-encrypted-project-data" aria-label="Permalink to “Searching encrypted project data”">​</a></h1><p>SysReptor stores sensitive pentest report content (e.g. findings, sections) encrypted in the database. This is a security measure: even a compromised database should not expose sensitive pentest data. But database encryption creates challenges: the database cannot run queries against ciphertext, so any operation on encrypted content (e.g. search) requires special handling.</p><p>This page explains how SysReptor implements full-text search over encrypted project data using a blind trigram index.</p><h2 id="the-problem-with-encrypted-fields" tabindex="-1">The problem with encrypted fields <a class="header-anchor" href="#the-problem-with-encrypted-fields" aria-label="Permalink to “The problem with encrypted fields”">​</a></h2><p>Most tools search content by running <code>LIKE &#39;%keyword%&#39;</code> or full-text search queries directly in the database. This works because the database can read the stored values in plaintext.</p><p>SysReptor encrypts sensitive project data at the application layer before it reaches the database. The database only ever sees ciphertext (i.e. opaque byte blobs). The encryption key is held by the application server, not the database.</p><p>The following options to resolve this problem turned out to be infeasible:</p><ul><li><strong>Decrypt everything in Python and filter</strong>: The application has to fetch and decrypt every finding in every project on every search request. With thousands of projects this is slow and resource-intensive.</li><li><strong>Disable encryption for project data</strong>: This defeats the purpose of encryption.</li><li><strong>Store a plaintext search index separately</strong>: Storing plaintext alongside ciphertext eliminates the security benefits of encryption. An attacker with database access would be able to read the index and reconstruct plaintext data.</li></ul><p>The solution has to let the database do the filtering without ever seeing the plaintext content.</p><h2 id="blind-trigram-index" tabindex="-1">Blind trigram index <a class="header-anchor" href="#blind-trigram-index" aria-label="Permalink to “Blind trigram index”">​</a></h2><p>SysReptor uses a <em>blind trigram index</em>: the database stores a set of opaque tokens per project, derived from the project&#39;s text contents. A search term is converted to tokens the same way, and the database matches projects by comparing token sets. The database stores only encrypted data and the blind index but never plaintext data.</p><h3 id="trigrams" tabindex="-1">Trigrams <a class="header-anchor" href="#trigrams" aria-label="Permalink to “Trigrams”">​</a></h3><p>A trigram is a set of three consecutive characters. The word <code>&quot;pentest&quot;</code> produces the trigrams <code>pen</code>, <code>ent</code>, <code>nte</code>, <code>tes</code>, <code>est</code>.</p><p>PostgreSQL&#39;s <code>pg_trgm</code> extension uses the same idea: if a search term occurs in a document, every trigram of that search term must also occur in the document. Containment over the set of trigrams is an efficient way to filter matches.</p><h3 id="hmac-based-blinding" tabindex="-1">HMAC-based blinding <a class="header-anchor" href="#hmac-based-blinding" aria-label="Permalink to “HMAC-based blinding”">​</a></h3><p>Each trigram is <em>blinded</em> by passing it through HMAC-SHA256, keyed with the application&#39;s encryption key:</p><div class="language- line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>token = HMAC-SHA256(key, &quot;sysreptor|blind_trigram|v1|&quot; + trigram_bytes)[:16]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Only those 16-byte tokens are stored, not the trigrams themselves.</p><ul><li>Tokens are deterministic. The same trigram always produces the same token under the same key, so equality lookups work.</li><li>Without the key, tokens are indistinguishable from random bytes. A read-only attacker on the database cannot reverse a token back to its trigram.</li><li>128 bits is enough that token collisions between distinct trigrams are negligible.</li></ul><p>The HMAC is keyed with the <strong>same application secret</strong> that encrypts the stored field blobs. That is a deliberate design choice, not an accident: anyone who can derive meaningful probes from the tokens already holds the key needed to decrypt the ciphertext directly, so the blind index does not introduce a weaker path than “the encryption key is compromised.”</p><h2 id="indexing" tabindex="-1">Indexing <a class="header-anchor" href="#indexing" aria-label="Permalink to “Indexing”">​</a></h2><p>An asynchronous background task runs regularly and rebuilds the blind trigram index for any project whose content has changed since the last run.</p><p>For each project, the process is:</p><ol><li><strong>Collect text</strong>: Every string field in every finding and section is extracted.</li><li><strong>Normalize</strong>: Strings are Unicode-normalized, case-folded, and whitespace-collapsed, which makes search case- and accent-insensitive.</li><li><strong>Compute trigrams</strong>: Normalized text is encoded as UTF-8 and split into sliding 3-byte windows. Trigrams are deduplicated across the project.</li><li><strong>Blind</strong>: Each unique trigram becomes a 16-byte token via the HMAC above.</li><li><strong>Store</strong>: All tokens are stored in the <code>BlindTrigramToken</code> table, one row per unique token, replacing any previous tokens for that project.</li></ol><h2 id="search" tabindex="-1">Search <a class="header-anchor" href="#search" aria-label="Permalink to “Search”">​</a></h2><p>A search term goes through the same normalization and trigram split as during indexing. Each trigram is HMAC&#39;d, and a project is considered a content match if its token set contains every token derived from the term. This is the same containment check <code>pg_trgm</code> performs over plaintext.</p><p>Search terms shorter than three characters skip the trigram lookup entirely. A 3-character term produces exactly one trigram; anything shorter has none.</p><h2 id="key-rotation" tabindex="-1">Key rotation <a class="header-anchor" href="#key-rotation" aria-label="Permalink to “Key rotation”">​</a></h2><p>The HMAC key follows the application&#39;s <code>DEFAULT_ENCRYPTION_KEY_ID</code>. SysReptor allows multiple encryption keys to be configured at once, so older data can still be decrypted while new data is written under a fresh key.</p><p>At search time, the term is hashed under every currently configured key, and rows matching any of them count as hits. A project that was indexed under an old key therefore remains searchable; the background task picks it up and rebuilds the index under the new key on its next pass.</p><h2 id="security-properties-and-limitations" tabindex="-1">Security properties and limitations <a class="header-anchor" href="#security-properties-and-limitations" aria-label="Permalink to “Security properties and limitations”">​</a></h2><p><strong>What it protects against</strong>: The blind index protects against read-only attackers with database access. They see 128-bit tokens that look random; without the HMAC key, recovering the underlying trigrams (or plaintext) is not feasible.</p><p><strong>Frequency analysis</strong>: The HMAC key is shared across all projects, so the same trigram always hashes to the same token. An attacker with database access can identify projects that share tokens. They do not recover any plaintext, but they do learn which projects have content in common.</p><p><strong>Dictionary attacks</strong>: With the application encryption key, an attacker can derive tokens for guessed terms (e.g. a CVE id) and query the index for matches. The HMAC uses that same key as the ciphertext, so the capability is not separate from key compromise: whoever can run this probe can decrypt field data directly. A read-only database attacker without the key cannot.</p><p><strong>False positives</strong>: A project that contains every trigram of a search term scattered across different words, but not the term itself, will still match. The effect is small for longer terms and is the same trade-off <code>pg_trgm</code> makes.</p><p><strong>Index lag</strong>: The index is rebuilt asynchronously, so a project that was just edited may not be findable under its new content for a few minutes.</p>`,37)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};